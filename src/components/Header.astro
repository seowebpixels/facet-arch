---
/* src/components/Header.astro */
import { Image } from 'astro:assets';
import MenuOverlay from './MenuOverlay.astro';
import logoImg from '../assets/facet-logo-white.avif';

const { pathname } = Astro.url;

// Explicitly check if current route is Home or Menu
const isHomePage = pathname === '/' || pathname === '';
const showLogoPages = ['/portfolio', '/about', '/contact'].some((path) => pathname.includes(path));

// White header logo ONLY renders on inner pages (brought down 20px)
const shouldShowLogo = !isHomePage && showLogoPages;
---

<header class="site-header">
  {shouldShowLogo && (
    <a href="/" class="logo-link" aria-label="Facet Architecture Home">
      <Image 
        src={logoImg} 
        alt="Facet Architecture Logo" 
        class="site-logo"
        loading="eager"
        decoding="sync"
      />
    </a>
  )}

  <button 
    id="menu-toggle" 
    class="menu-box" 
    aria-label="Toggle Navigation Menu" 
    aria-expanded="false"
    onclick="
      const overlay = document.getElementById('menu-overlay');
      const isExpanded = this.getAttribute('aria-expanded') === 'true';
      this.setAttribute('aria-expanded', (!isExpanded).toString());
      if (overlay) {
        overlay.classList.toggle('active', !isExpanded);
        document.body.classList.toggle('menu-open', !isExpanded);
      }
    "
  >
    <div class="hamburger-lines">
      <span class="line line-1"></span>
      <span class="line line-2"></span>
      <span class="line line-3"></span>
    </div>
  </button>
</header>

<MenuOverlay />

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 0;
    z-index: 2147483646;
    pointer-events: none;
  }

  /* Inner Pages White Logo: Brought down 20px */
  .logo-link {
    pointer-events: auto !important;
    position: fixed;
    top: 4px; /* Shifted down 20px on Desktop */
    right: 20px;
    left: auto;
    z-index: 2147483647;
    display: flex;
    align-items: center;
    text-decoration: none;
  }

  .site-logo {
    height: clamp(125px, 14vw, 155px);
    width: auto;
    display: block;
    transition: transform 0.3s ease;
  }

  .site-logo:hover {
    transform: scale(1.04);
  }

  /* TOGGLE BOX */
  .menu-box {
    pointer-events: auto !important;
    position: fixed;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    z-index: 2147483647;
    width: 64px;
    height: 72px;
    background-color: #7c7a7a;
    border: none;
    cursor: pointer;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    box-sizing: border-box;
  }

  .hamburger-lines {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: center;
    width: 28px;
    height: 18px;
    pointer-events: none;
  }

  .line {
    width: 100%;
    height: 2px;
    background-color: #000000;
    transition: transform 0.3s ease, opacity 0.3s ease;
    pointer-events: none;
  }

  .menu-box[aria-expanded="true"] .line-1 {
    transform: translateY(8px) rotate(45deg);
  }

  .menu-box[aria-expanded="true"] .line-2 {
    opacity: 0;
  }

  .menu-box[aria-expanded="true"] .line-3 {
    transform: translateY(-8px) rotate(-45deg);
  }

  @media (max-width: 768px) {
    .logo-link {
      right: 12px;
      top: 8px; /* Shifted down 20px on Mobile */
    }

    .menu-box {
      width: 52px;
      height: 60px;
    }

    .hamburger-lines {
      width: 24px;
      height: 16px;
    }

    .menu-box[aria-expanded="true"] .line-1 {
      transform: translateY(7px) rotate(45deg);
    }

    .menu-box[aria-expanded="true"] .line-3 {
      transform: translateY(-7px) rotate(-45deg);
    }
  }
</style>